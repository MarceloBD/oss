# Migration `watch-20190902031645`

This migration has been generated by marcelobd at 9/2/2019, 3:16:45 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190902031645
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,24 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://auror:auror@localhost:5432/auror?schema=public"
+}
+
+model KnexMigration {
+  id            Int       @id
+  batch         Int?
+  migrationTime DateTime? @map("migration_time")
+  name          String?
+
+  @@map("knex_migrations")
+}
+
+model User {
+  id   Int     @id
+  name String?
+
+  @@map("user")
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190902031645)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190902031645'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
